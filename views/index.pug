extends layout.pug

block content
  article.grid-container.portfolio-intro
    div

      h1 My Portfolio!

      p.lead.text-light
        | This is my portfolio for all the Treehouse Projects I have completed so far, Im hoping to become a great web developer so I can increase the horizon of skills I have accomplished in the computer software department!
  article.grid-container.portfolio-index

    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

      each project, index in projects
        .cell
          a(href=`/project/${index}`)
            img.thumbnail(src=`/static/${project.image_urls[0]}`)
            h5 #{project.project_name}
